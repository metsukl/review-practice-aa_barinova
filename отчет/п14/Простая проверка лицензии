import os  # Импортируем модуль os для работы с файловой системой

def check_license():  # Определяем функцию для проверки лицензии
    if not os.path.exists("license.key"):  # Проверяем, существует ли файл лицензии
        print("Файл лицензии не найден!")  # Выводим сообщение, если файл не найден
        return False  # Возвращаем ложь, если файл отсутствует
    
    with open("license.key") as f:  # Открываем файл лицензии для чтения
        key = f.read().strip()  # Читаем содержимое и убираем пробелы по краям
        return key == "ABCD-1234-EFGH"  # Сравниваем с корректным ключом

def main():  # Определяем основную функцию программы
    if not check_license():  # Вызываем функцию проверки лицензии
        print("Программа заблокирована!")  # Если лицензия неверна, выводим сообщение
        return  # Завершаем выполнение программы
    
    print("Программа запущена!")  # Если лицензия корректна, запускаем основную часть программы
    # Основной код

if __name__ == "__main__":  # Проверяем, запущена ли программа непосредственно
    main()  # Если да, вызываем основную функцию
### Результаты работы программы

1. Нет файла лицензии:
   - Программа выводит сообщение "Файл лицензии не найден!" и блокирует выполнение, то есть ничего больше не происходит.

2. Есть файл лицензии, но ключ неверный:
   - Программа считает, что файл существует. Если в файле лицензии содержится, например, "XYZ-5678-QRST", она выведет "Программа заблокирована!" и завершит выполнение.

3. Лицензия соответствует:
   - Если файл лицензии содержит "ABCD-1234-EFGH", программа выведет "Программа запущена!" и продолжит выполнение основного кода.
